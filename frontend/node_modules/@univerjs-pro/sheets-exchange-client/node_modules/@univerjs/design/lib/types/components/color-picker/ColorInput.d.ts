import { default as React } from 'react';
interface IColorInputProps {
    hsv: [number, number, number];
    alpha: number;
    showAlpha?: boolean;
    onChangeColor: (h: number, s: number, v: number) => void;
    onChangeAlpha: (alpha: number) => void;
}
export declare function ColorInput({ hsv, alpha, showAlpha, onChangeColor, onChangeAlpha }: IColorInputProps): React.JSX.Element;
export {};
