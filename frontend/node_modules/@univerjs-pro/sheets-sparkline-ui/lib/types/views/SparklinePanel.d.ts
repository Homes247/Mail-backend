import { IRange, ISelectionCell } from '@univerjs/core';
import { default as React } from 'react';
export interface ISparklinePanelCtx {
    unitId: string;
    subUnitId: string;
    range: IRange;
    primary: ISelectionCell;
    groupId: string;
}
export declare const SparklinePanel: () => React.JSX.Element | null;
