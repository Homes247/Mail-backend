import { default as React } from 'react';
export interface IColorPickerControlProps {
    color?: string;
    onChange?: (color: string) => void;
}
export declare const ColorPickerControl: (props: IColorPickerControlProps) => React.JSX.Element;
