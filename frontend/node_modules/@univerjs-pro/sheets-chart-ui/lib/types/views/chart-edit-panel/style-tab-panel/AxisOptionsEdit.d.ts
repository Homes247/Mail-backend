import { DeepPartial, IAxisOptions } from '@univerjs-pro/sheets-chart';
import { LocaleService, Nullable } from '@univerjs/core';
import { default as React } from 'react';
export interface IAxisOptionsEditProps {
    localeService: LocaleService;
    valueAxis: boolean;
    axisOptions: Nullable<DeepPartial<IAxisOptions>>;
    onChange?: (axisOptions: DeepPartial<IAxisOptions>) => void;
}
export declare const AxisOptionsEdit: (props: IAxisOptionsEditProps) => React.JSX.Element;
