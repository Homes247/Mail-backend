import { default as React } from 'react';
export interface IColorPickerProps {
    format?: 'hex';
    value?: string;
    showAlpha?: boolean;
    onChange?: (value: string) => void;
}
export declare function ColorPicker({ format, value, showAlpha, onChange }: IColorPickerProps): React.JSX.Element | null;
