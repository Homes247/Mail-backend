import { default as React } from 'react';
export interface IPagerProps {
    text?: string;
    value: number;
    total: number;
    loop?: boolean;
    onChange?(value: number): void;
}
export declare function Pager(props: IPagerProps): React.JSX.Element;
