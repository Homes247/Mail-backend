import { default as React } from 'react';
export interface IExtendConfirmProps {
    onChange: (value: string) => void;
}
export declare const ExtendConfirm: (props: IExtendConfirmProps) => React.JSX.Element;
