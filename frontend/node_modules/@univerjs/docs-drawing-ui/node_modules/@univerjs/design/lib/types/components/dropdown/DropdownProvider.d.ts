import { ReactNode, default as React } from 'react';
interface IDropdownProviderProps {
    visible?: boolean;
    children: ReactNode;
    disabled?: boolean;
    onVisibleChange?: (visible: boolean) => void;
}
export declare function DropdownProvider({ visible, children, disabled, onVisibleChange }: IDropdownProviderProps): React.JSX.Element;
export {};
