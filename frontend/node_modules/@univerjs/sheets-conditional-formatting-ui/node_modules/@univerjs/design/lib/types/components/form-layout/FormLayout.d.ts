import { PropsWithChildren, default as React } from 'react';
export interface IFormLayoutProps {
    label?: React.ReactNode;
    desc?: React.ReactNode;
    children?: React.ReactNode;
    style?: React.CSSProperties;
    className?: string;
    contentStyle?: React.CSSProperties;
    error?: string;
    collapsable?: boolean;
    defaultCollapsed?: boolean;
}
export declare const FormLayout: (props: IFormLayoutProps) => React.JSX.Element;
export type IFormDualColumnLayoutProps = PropsWithChildren;
/**
 * A dual columns layout component for the form.
 * @param props props of the component
 */
export declare const FormDualColumnLayout: (props: IFormDualColumnLayoutProps) => React.JSX.Element;
