import { default as React } from 'react';
export interface IPopupProps {
    children: React.ReactElement;
    /**
     * whether popup is visible
     * @default false
     */
    visible?: boolean;
    /**
     * the offset of popup
     * @default [0, 0]
     */
    offset?: [number, number];
}
export declare function Popup(props: IPopupProps): React.ReactPortal;
