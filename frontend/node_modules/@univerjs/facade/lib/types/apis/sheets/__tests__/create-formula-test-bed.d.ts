import { Dependency, IWorkbookData, Workbook, Injector, Univer } from '@univerjs/core';
import { FUniver } from '../../everything';
export interface ITestBed {
    univer: Univer;
    get: Injector['get'];
    sheet: Workbook;
    univerAPI: FUniver;
}
export declare function createFormulaTestBed(workbookData?: IWorkbookData, dependencies?: Dependency[]): ITestBed;
