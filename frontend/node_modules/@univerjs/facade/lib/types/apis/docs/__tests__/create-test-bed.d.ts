import { Dependency, DocumentDataModel, IDocumentData, Injector, Univer } from '@univerjs/core';
import { FUniver } from '../../everything';
export interface ITestBed {
    univer: Univer;
    get: Injector['get'];
    doc: DocumentDataModel;
    univerAPI: FUniver;
}
export declare function createTestBed(documentConfig?: IDocumentData, dependencies?: Dependency[]): ITestBed;
