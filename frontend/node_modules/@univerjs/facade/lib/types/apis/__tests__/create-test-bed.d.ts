import { Dependency, IWorkbookData, UnitModel, Injector, Univer } from '@univerjs/core';
import { FUniver } from '../everything';
export interface ITestBed {
    univer: Univer;
    get: Injector['get'];
    sheet: UnitModel<IWorkbookData>;
    univerAPI: FUniver;
    injector: Injector;
}
export declare function createFacadeTestBed(workbookData?: IWorkbookData, dependencies?: Dependency[]): ITestBed;
