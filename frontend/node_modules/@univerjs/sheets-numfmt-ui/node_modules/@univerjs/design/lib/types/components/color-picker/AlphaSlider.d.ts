import { default as React } from 'react';
interface IAlphaSliderProps {
    hsv: [number, number, number];
    alpha: number;
    onChange: (alpha: number) => void;
    onChanged?: (alpha: number) => void;
}
export declare const AlphaSlider: React.FC<IAlphaSliderProps>;
export {};
